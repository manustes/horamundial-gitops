apiVersion: v1
kind: Service
metadata:
  name: {{ include "horamundial.fullname" . }}
  labels:
    {{- include "horamundial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "horamundial.selectorLabels" . | nindent 4 }}